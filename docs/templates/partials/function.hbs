## {{capitalize name}}


{{summaryOrDescription .}}

```javascript
{{#if (isConstructor .)~}}new {{else}}{{~#unless (isGlobal .)~}}{{lowercase memberof}}.{{~/unless~}}{{/if~}}
{{~name~}}
{{~#if this.params~}}
  (
  {{~#each params~}}
    {{~#if (isTopLevelParam name)}}
      {{~name~}}
      {{~#unless @last~}}, {{/unless~}}
    {{~/if~}}
  {{~/each~}}
  )
{{else}}
()
{{/if}}
```

{{>paramlist}}

{{>returnlist}}

{{descriptionIfSummary .}}

{{>examples}}
